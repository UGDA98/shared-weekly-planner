# NOTES.md — יומן ומשימות (מערכת לוז ומשימות, אוגדה 98)

Single source of truth for resuming this project cold. Update this file in place whenever architecture, state, or decisions change — don't let it drift.

## Goal

A shared weekly planner/task board for the education staff of אוגדה 98 (Hebrew, RTL) — replaces an original Excel file. **This is a live production tool with real users entering real data**, not a dev sandbox.

- Live site: https://ugda98.github.io/shared-weekly-planner/

## Architecture & Stack

The system spans **two separate local folders on this machine — do not confuse them**:

| Piece | Local path | Repo/tooling |
|---|---|---|
| Frontend | `/Users/createuser/Desktop/claude projects/02-luz-umesimot/frontend/` | git repo `UGDA98/shared-weekly-planner`, branch `main`, deployed via GitHub Pages straight from the repo |
| Backend | `/Users/createuser/Desktop/claude projects/02-luz-umesimot/backend-apps-script/` | **not a git repo** — a `clasp`-managed Google Apps Script project (`clasp` is authenticated on this machine) |

> Paths updated 2026-09-09: both folders were previously at `~/Desktop/מערכת לוז ומשימות/` and `~/Desktop/מערכת לוז ומשימות - גב/` and have since moved under one parent. They are still two **separate** folders with separate deploy paths — do not merge them.

**Frontend**: single self-contained `index.html` (5075 lines) — no build step, no external dependencies beyond one `<script src="https://accounts.google.com/gsi/client">` tag (Google Identity Services, for sign-in only). Rubik font is embedded as base64 inside the CSS. Entire JS is one IIFE inside one `<script>`; entire CSS is one `<style>` block (`:root` design tokens at lines ~19–56: `--bg`, `--surface`, `--accent`, `--ink`, `--border`, `--radius-*`, etc. — dark-mode CSS exists but is force-disabled in JS, light mode is always active).

**Backend**: Google Apps Script Web App (`קוד.js`, ~1020 lines) bound to a Google Sheet.
- Web App URL (fixed, referenced in frontend as `API_URL` at ~[index.html:838](index.html#L838)): `https://script.google.com/macros/s/AKfycbxO8tTn_6c1CxqcFSB35kgjQ3LYrmdzfjixTEdADlLCYyPB7sgL3Kw-UgCdwVXCkUU/exec`
- Script ID: `1ldjlbM_ccZDoF9YuFvtk8_7KuAJnBXA6N9xusBWAFxr5CCnwmNTcPpSN`. Container-bound to the Google Sheet — **does not appear in script.google.com's "My projects" list**; open it directly via `https://script.google.com/d/1ldjlbM_ccZDoF9YuFvtk8_7KuAJnBXA6N9xusBWAFxr5CCnwmNTcPpSN/edit` (must be the same Google account the web app is deployed to execute as).
- Deploy flow: edit in backend folder → `clasp push` → `clasp version "<description>"` → `clasp deploy --deploymentId AKfycbxO8tTn_6c1CxqcFSB35kgjQ3LYrmdzfjixTEdADlLCYyPB7sgL3Kw-UgCdwVXCkUU --versionNumber <new>`. **Must reuse this exact deploymentId** or the live site's API URL breaks. Currently deployed: **v19**.
- `doGet`/`doPost` route on an `action` param: `listBoards`, `getBoard`, `listAccess`, `getActivity` (GET); `saveBoard`, `createBoard`, `renameBoard`, `deleteBoard`, `login`, `logout`, `setAccess`, `removeAccess`, `setDefaultRole`, `setScriptConfig` (POST). No `action` at all routes to `LEGACY_BOARD_ID` (`board-1`) for backward compatibility with old cached tabs.

**Google sign-in (added 2026-09-16)**: every action except `login`/`logout` is routed through `resolveIdentity(params)` before its handler runs. A request identifies itself either by a `session` token (from `login`, backed by a Google ID token verified server-side against `oauth2.googleapis.com/tokeninfo`) or, only while the `REQUIRE_AUTH` Script Property is not `'true'`, by a plain `email` field — the old email-gate client's exact shape, kept working during rollout. `GOOGLE_CLIENT_ID` and `REQUIRE_AUTH` live in Script Properties (`PropertiesService`), settable either from the Apps Script editor (`adminSetClientId`/`adminSetRequireAuth`) or over the API via `setScriptConfig` (gated to board-1 editors — see Decisions). Sessions live 90 days in a new `Sessions` sheet tab (`Token|Email|CreatedAt|ExpiresAt|LastSeenAt|UserAgent`), cached in `CacheService` (6h TTL) so most requests skip the sheet scan; `LastSeenAt` updates at most once/hour per session. A new `Log` tab (`Timestamp|Email|Action|Board|Result|Details|Auth`) is an audit trail — `login`/`login_failed`/`logout`/`saveBoard`/`createBoard`/`renameBoard`/`deleteBoard`/`setAccess`/`removeAccess`/`setDefaultRole`/`setScriptConfig`, trimmed to the newest 8,000 rows. Frontend: `authQuery()`/`authBody()` (~[index.html:1027](index.html#L1027)) attach the session to every call; `handleAuthError(json)` (~[index.html:1036](index.html#L1036)) routes a `session_expired`/`auth_required` response back to the sign-in card. Access management (list/add/change-role/remove/default-role) and a recent-activity view live in the drawer, editors only — `renderAccessList`/`renderActivityList` (~[index.html:1486](index.html#L1486)/[1594](index.html#L1594)).

**Data model (multi-board, schema-less)**: Google Sheet with tabs `Boards` (BoardId/Name/DefaultRole/CreatedBy/CreatedAt/**Type**/State-JSON, **plus `State2`…`StateN` chunk columns created on demand**), `Config` (Board/Email/Role, per-board access control), `Sessions`, `Log` (both above). Each board's entire app state is one JSON blob that round-trips as-is through `saveBoard`/`getBoard`/`listBoards`/`createBoard`/`renameBoard`/`deleteBoard` — the backend doesn't understand the shape, just stores/returns it. `Boards.Type` (`planner`/blank or `notes`) selects the board type; `createBoard`/`listBoards`/`getBoard` accept/return it and `defaultBoardState(type)` branches on it (backend, v11+). The frontend picks the type in the new-board modal and branches its entire render on `currentBoardType` — see "Notes boards" below.

**Notes boards** (second board type, added this session): a Notion-style gallery of pages, each an arbitrarily nested outline. Selected via a type-choice card in the new-board modal (`.modal-type-choice`); `currentBoardType` ('planner' | 'notes') drives `renderAll()` to branch between the week board and `renderNotes()` entirely — no shared rendering code between the two types beyond `showUndoToast`/`showConfirmModal`/`persistState`/`stripHtmlPreview`/`relTime`, which are the app's general-purpose primitives already used by the planner side. Shape: `appState = { kind:'notes', pages:[Page] }`, `Page = { id, title, createdAt, updatedAt, blocks:[Block] }`, `Block = { id, html, style:'p'|'h1'|'h2', collapsed, children:[Block] }`. `html` is restricted to `b/i/u/s/br` with no attributes (`sanitizeInline()`, ~[index.html:4340](index.html#L4340)) — enforced on every commit **and** re-applied on every load (`sanitizeNotesState()`) as defense against a write from anything other than this client, since the backend still treats the whole blob as opaque. Collapse state (`Block.collapsed`) is stored in the blob, not per-user `localStorage` like `makeCollapsible` — deliberately, since a collapsed section is content the gallery preview also needs to reflect (see Decisions). No drag-and-drop: all structural editing (indent/outdent/reorder/split/merge) is keyboard shortcuts or a touch toolbar, matching the existing "click-based, not drag-based" pattern for day-assignment (Decisions, `openDayPicker`).

**State chunking (added 2026-09-09)**: a Sheets cell holds max 50,000 characters and `appState.weeks` is never pruned, so the blob outgrows one cell within months of normal use. `saveBoardStateRaw` splits the JSON into ≤45,000-char chunks across `State`, `State2`, … (`ensureStateColumns` appends missing header cells; `stateColIndexes` resolves them), and `getBoardState` concatenates them back. Over `MAX_STATE_CHUNKS` (12, ≈540k chars) it writes **nothing** and returns `state_too_large` rather than corrupting the board with a partial write.

**Day model**: days are `sun, mon, tue, wed, thu, fri, sat` (`DAY_DEFS`). Friday and Saturday were merged into one `weekend` bucket until 2026-09-09 — see Decisions and Watch Out #12.

**Frontend data flow / sync**: poll server every 20s (`POLL_MS`, ~[index.html:702](index.html#L702)) via `refreshFromServer()` (~[index.html:803](index.html#L803)); local edits save to `localStorage` immediately + debounce 600ms before POSTing to server (`persistState`/`pushState`, ~[index.html:752](index.html#L752)/[769](index.html#L769)); immediate flush via `navigator.sendBeacon` on `visibilitychange`/`pagehide`; one-step-back backup always kept in `localStorage` with a "restore from last backup" button in the admin menu.

**PWA (added 2026-09-17)**: `manifest.webmanifest` + `frontend/icons/` (source `icon.svg`/`icon-maskable.svg`, rasterized via `sharp` to `icon-192.png`/`icon-512.png`/`icon-maskable-512.png`/`apple-touch-icon.png` — regenerate all four together if the icon ever changes, they must stay visually consistent). Linked from `<head>`: `<link rel="manifest">`, `<link rel="icon">` (SVG favicon), `<link rel="apple-touch-icon">`, plus `theme-color`/`apple-mobile-web-app-title`/`apple-mobile-web-app-capable`/`mobile-web-app-capable` meta tags. **No service worker** — see Decisions.

**Board switching**: `?board=<id>` URL param (read in `resolveInitialBoardId()`, ~[index.html:876](index.html#L876)) takes priority over the last-used board cached in `localStorage`; falls back to `LEGACY_BOARD_ID`. `switchToBoard()` (~[index.html:1149](index.html#L1149)) handles the actual swap.

**Deployment behavior**: frontend push → live in ~1-2 min, but GitHub Pages serves with `max-age=600` (~10 min cache) — a stale-looking page right after push is usually cache, not a bug; hard-refresh or wait.

### Key frontend functions (verify with `grep -n` before editing — line numbers drift)

| Area | Functions | ~Lines |
|---|---|---|
| Auth (session) | `authQuery()`, `authBody(obj)` — every API call's auth attachment; `handleAuthError(json)` — routes `session_expired`/`auth_required` back to the sign-in card | 1027–1057 |
| Google sign-in UI | `showSigninCard(opts)`, `waitForGsi(timeoutMs)`, `initGoogleSignin(migrate)`, `onGoogleCredential(response)` — GIS init/render + the `login` round-trip | search `function initGoogleSignin` |
| Audit-trail change note | `summarizeChange(prevState, nextState)`, `collectChangeItems`, `stripHtmlPreview`, `dayLabel` — diffs two state blobs by item id, called once per save inside `pushState` | 1123–1180 |
| Access management (drawer, editors only) | `renderAccessList`, `callSetAccess`, `callRemoveAccess`, `callSetDefaultRole` | 1486–1560 |
| Activity log (drawer, editors only) | `renderActivityList`, `relTime(iso)`, `ACTIVITY_ACTION_LABELS` | 1594–1650 |
| Notes: board-type plumbing | `defaultStateFor`, `isValidStateFor`, `cachedBoardType`/`cacheBoardType` | search `function isValidStateFor` |
| Notes: gallery | `renderNotesGallery`, `renderNotesCard`, `createNewNotesPage`, `deletePageWithUndo` | search `function renderNotesGallery` |
| Notes: outline editor | `renderBlockRow`, `wireOutlineTextEvents`, `sanitizeInline`, `splitBlockAtCaret`, `indentBlock`/`outdentBlock`, `mergeIntoPrevious`, `moveBlockAmongSiblings`, `toggleCollapse`, `findBlockCtx`, `flattenVisibleBlocks` | search `function findBlockCtx` |
| Data model / migration | `emptyTask`, `migratePersonIds`, `migrateLegacy`, `loadWeek` | 2022–2067 |
| Undo toast | `showUndoToast(message, onUndo)`, `hideUndoToast()` — one toast at a time, ~6s auto-dismiss, wired into all 4 delete sites below | 1962–1990 |
| Filters / summary | `itemMatchesFilter`, `applyFilterVisuals`, `itemMatchesKind`, `collectItemsFor`, `renderSummaryRow`, `renderSummaryGroup`, `renderSummary` | 2215–2470 |
| Task pool | `poolItems`, `renderPoolRow`, `renderPoolCard` | 2473–2600 |
| Person/topic picker | `openPicker(kind, anchorEl, currentId, onPick, opts)` — `opts.multi` for multi-select | 2780–3020 |
| Drag system | `setupDrag(handle, listEl, getArray, rowSelector, onReorder, opts)`, `endActiveDrag`, `updateBoardAutoScroll` (ramped edge-scroll speed) — `opts.dropzone` for cross-container drag, `opts.threshold` for delayed-drag (e.g. off a checkbox); `dragHandleEl()` | 3021–3130 |
| Collapse (shared) | `readCollapseState`, `writeCollapseState`, `makeCollapsible(headEl, bodyEl, storageKey, itemCount)` — `COLLAPSE_AUTO_THRESHOLD = 6` | 3133–3147 |
| Board render | `renderDay`, `renderDayBanner`, `renderEventRow`, `renderTaskRow`, `renderBoard` — each delete handler now wires `showUndoToast` | 3206–3379 |
| Day title (pinned banner) | `renderDayBanner(dayDef, data)` — renders `.day-banner` (filled, `contentEditable` via `makeEditable`) or `.day-banner-add` (`.add-line`-style, empty state); reads/writes `data.title` on the day object; called from `renderDay` right after the `.day-head` append, before `.zone-events` — sibling of both, so it's invisible to `setupDrag`'s dropzone scan and to the `.zone-events` height-equalization (see Decisions) | search `function renderDayBanner` |
| Event-zone height equalization | `equalizeEventZoneHeightsNow`, `equalizeEventZoneHeights`, `watchEventZoneSizes` (ResizeObserver) — `.zone-events` now has a `min-height` CSS transition so corrections settle instead of snapping | search `function equalizeEventZoneHeights` |
| Board list / sync | `fetchBoardsList`, `switchToBoard`, `refreshFromServer`, `startPolling` | 1200–1264, 1782 |
| Wire-chip-reveal (task metadata) | `wireChipReveal(row, main, isAssigned, labelText)` — also renders the always-visible `.task-flag-label` (assignee name/count) next to the flag dot, not just the flag itself | 3379 |
| Day-assignment from summary/pool views | `openDayPicker(anchorEl, currentDayKey, onPick)` — small fixed-list popover (modeled on `openModePicker`, not the editable-list `openPicker`) listing `DAY_DEFS` + "ללא"; `moveTaskToDay(item, fromDayKey, fromSectionKey, toDayKey)` — moves an item between `appState.unscheduled` and `weekData[day].general`. Wired into a day chip (`ICON_CALENDAR`) in both `renderSummaryRow` and `renderPoolRow`, always clickable (also on tasks already in a day, to reassign/unassign). `renderSummaryRow`'s "other kind" chip (person↔topic) is now a real `openPicker`-backed chip too, not a static label — previously neither was possible from a person/topic/pool card | 2699–2717 |

### Reusable patterns (use these, don't build parallel mechanisms)

- **Collapse**: any new collapsible UI → use `makeCollapsible`.
- **Drag**: `setupDrag` is the only drag API in the file (`opts.dropzone` for cross-container, `opts.threshold` when the handle overlaps another interactive element like a checkbox).
- **Person/topic tagging**: `makeMetaChip(icon, filled)` + `openPicker(...)`.
- **Destructive row actions**: any new delete affordance → call `showUndoToast(message, onUndo)` instead of `confirm()`/a modal — matches the pattern already used for tasks/events/pool/summary-row deletes (see Decisions).

## Current State

Everything below is built, live, and verified on `main`:

- **PWA home-screen icon** (this session, 2026-09-17): `manifest.webmanifest` + a designed icon (purple checklist glyph matching `--accent`) at every size Android/iOS need, `display:"standalone"`. "Add to Home Screen" now shows the real icon instead of a blank/default one. No service worker (Decisions). Verified: manifest + every icon file serve 200 with correct content-types on the live URL; the literal on-device "Add to Home Screen" tap could not be verified (no full Xcode install on this machine for the iOS Simulator) — worth a manual one-time check on an actual phone.
- **Notes boards** (this session, 2026-09-17): a second, selectable board type — a gallery of pages, each an infinitely-nestable bulleted outline with heading emphasis (`p`/`h1`/`h2`), inline bold/italic/underline/strikethrough, collapse/expand, and keyboard-driven structural editing (Enter/Tab/Shift+Tab/Backspace-merge/Alt+Shift+move) plus a touch toolbar for mobile. Chosen at board creation via a type-choice card in the new-board modal; existing planner boards are completely unaffected (`currentBoardType` gates every render path). See Architecture "Notes boards" for the data shape and `sanitizeInline`/`sanitizeNotesState` for the content-safety model. Deliberately click/keyboard-only, no drag-and-drop, matching the project's existing preference for click-based interactions over cross-subtree drag (Decisions).
- **Google sign-in, per-board access management, and an activity log** (this session, 2026-09-16/17): replaces the free-text email-gate. Sign-in card uses Google Identity Services' rendered button in FedCM mode (`use_fedcm_for_button:true` — required, see Watch Out); a session token (90-day, `Sessions` sheet tab) authenticates every subsequent call. Drawer gained two editors-only sections: "הרשאות ללוח הזה" (list/add/change-role/remove per-email access, plus "כל מי שיש לו קישור" default role) and "פעילות אחרונה" (recent login/save/create/rename/delete/access-change events from the new `Log` sheet tab, with a short auto-generated change note per save — `summarizeChange`). Rollout is a **grace period**: the backend accepts both a Google session and the old plain `email` param until the `REQUIRE_AUTH` Script Property is flipped to `'true'` (see Decisions and the rollout close-out step below) — not yet flipped as of this writing. `Boards.Type` column also added this session, now consumed by the notes board type above.
- **Full person/topic/day assignment from every task entry point** (this session): a task created from a person's card or a topic's card in "המשימות של כל אחד" (`renderSummaryRow`), or from "מאגר משימות" (`renderPoolRow`), can now be linked to the *other* dimension (person↔topic) and to a specific day of the current week — all via clickable chips, matching the chips already available on a task inside a day (`renderTaskRow`). Previously these entry points could only set the one dimension implied by which card you clicked into, with no way to add the other one or a day at all. A day chip on an already-day-assigned task (shown in a summary card) can also reassign or unassign it. See `openDayPicker`/`moveTaskToDay` above.
- **Day title (pinned banner)** (this session): each day card can carry a free-text title pinned above the events zone (`data.title` on the day object) — for "the whole day is X" cases (training, leave, etc.) without occupying an hourly event slot. Empty state shows a "+ הוספת כותרת ליום" add-line matching the existing "+ הוספת אירוע"/"+ הוספת משימה" pattern; filled state shows a bold `--accent`/`--accent-ink` banner, always `contentEditable` (same pattern as event/task text); clearing the text reverts it to the add-line automatically. No backend change needed — travels as part of the existing schema-less per-board JSON blob. Backward-compatible via `migrateLegacy`.
- Multi-board system: create/rename/delete boards, per-board editor/viewer roles, board switcher drawer (RTL-correct).
- Person/topic customization: drag-to-reorder, confirm-before-delete when in use.
- Multi-person task assignment (`personId` → `personIds[]`, auto-migrated via `migrateLegacy`).
- Cross-container drag (events/tasks between days, sections, and the task pool) via `data-dropzone` + board auto-scroll at edges.
- Permanent "מאגר משימות" (task pool) card for unscheduled tasks.
- Drag directly from a task's checkbox (not a separate handle) — `setupDrag` with `threshold:7px`, suppresses the synthetic click after a drag.
- Shared collapse/expand for day sections ("משימות"/"צוות") and summary cards ("לפי אדם"/"לפי מופע") — auto-closed above 6 items, manual choice always wins and persists.
- Person/topic tags hidden behind a small dot indicator until clicked (dropdown), to declutter task rows.
- Equal height across all "white events box" zones in a week — handled by a `ResizeObserver` (see Decisions) instead of fixed-timing correction passes; the correction now transitions smoothly instead of snapping (this session).
- Data-loss prevention: `sendBeacon` flush on tab close/hide, one-step-back backup + restore button, **plus a per-action undo toast** (this session) on every task/event/pool/summary-row delete — delete stays instant, ~6s to reverse it.
- Task assignment is always visible, not just on click: a compact name/count label sits next to the flag dot at all times (`.task-flag-label`, this session) — the dot alone used to be the only always-visible signal.
- Section/group item counts (`.collapse-count`, `.summary-group-count`) now decrement on delete, not just increment on add (this session) — they used to go stale until the next full re-render.
- Touch/mobile UX: always-visible delete/rename buttons (not hover-only), `pointer:coarse` breakpoints, popover vertical clamping, no double-scroll, correct RTL nav arrows, checkbox at the 24px WCAG minimum, collapse-toggle hit area grown to 24px via padding+negative-margin (visually unchanged) — all this session's additions on top of the pre-existing touch pass.
- Contrast pass (this session, WCAG AA): `--muted` darkened `#867e97→#6a627d`; done-task text no longer stacks `opacity:.7` on top of it (was ~2.2:1, now passes); unchecked-checkbox border reuses `--muted` instead of the shared `--border-strong` (scoped, so decorative dividers elsewhere stay as light as before); `--today-ring` nudged `#c9922e→#b8842a`, same hue, now passes the 3:1 UI-component minimum.
- Drag feedback and drop-zone highlighting now transition (opacity/background/outline-color, ~150ms) instead of snapping instantly; drag auto-scroll ramps in with proximity to the board edge instead of jumping straight to full speed (this session — motion review, no changes to the reorder/commit logic itself).
- Dark mode forced off (always light).
- Wider day cards (300px; the 330px `.day-wide` weekend card is gone with the fri/sat split).
- **Seven day columns** — שישי and שבת are separate days; the merged `weekend` bucket is legacy-only (this session).
- **Month view** (this session): 7-column RTL grid, one row per existing week key, read-only chips; clicking a day opens a **day peek** (bottom sheet on mobile, anchored popover on desktop) that reuses `renderEventRow`/`renderTaskRow` verbatim for full editing. Week/month toggle in the top bar, `?view=month` URL param, per-user-per-board persistence. Summary and highlight filter both follow the view's scope.
- **Save failures are now surfaced** — `pushState` reads the response instead of always reporting `מסונכרן ✓` (this session).
- **"Already happened today" toggle on events** (this session): `renderEventRow` gained a small icon toggle (`item.passed`) between the drag handle and the text. Deliberately not a checkbox and not a strikethrough — those belong to `item.done`/`renderTaskRow` only. First pass tinted the row with an `--accent-soft` background box, which real user feedback caught immediately: it read as *emphasis* (look at this), the opposite of "this is over." Fixed to a row-wide opacity fade (`.55`, easing to `.85` on hover) with a plain-`--ink` toggle icon and no background tint — the row recedes instead of getting boxed. Fully reversible, no `showUndoToast` (not a delete), hidden in `view-only` mode like `.row-del`. No backend/migration change — `item.passed` is read via `!!item.passed`, same as `item.done`. Shared automatically by the month-view day-peek since both render paths call the same `renderEventRow`.

**Explicitly rejected, do not re-propose**: a free-text "הערות" (notes) section on the board — raised previously, explicitly removed, nothing implemented. Also rejected this session: replacing the checkbox-as-drag-handle with a separate visible handle icon (would touch working drag wiring for a P3 discoverability issue) — used a `title` tooltip on the checkbox instead.

## Decisions

| Decision | Why | Alternative considered & why rejected |
|---|---|---|
| Single self-contained `index.html`, no build step | Simplicity for a small, single-page tool; easy to deploy straight to GitHub Pages | — |
| Google Sheets (via Apps Script) as the data store, not a real database | Keeps existing org data location, avoids new infra | An earlier, now-abandoned early plan considered calling the Sheets API directly from the frontend — rejected in favor of an Apps Script Web App middle layer for access control per board (`Config` tab) |
| Schema-less JSON blob per board, not normalized rows | Fast to iterate on frontend data shape without backend migrations; backend doesn't need to understand the app's structure | — |
| 20s poll + optimistic local writes + debounce, over websockets/real-time push | Simple, no extra infrastructure; acceptable staleness window for this use case | — |
| Dark mode force-disabled | Product decision for consistency (see git history) | — |
| Multi-board model (`Boards`/`Config` tabs) over one shared sheet | Lets each unit/person have their own board with its own access list | Legacy single-board (`board-1`) kept working via the no-`action` fallback path, for old cached tabs |
| Event-zone height equalization: `ResizeObserver` watching each day's `.events` list, not fixed-timing passes | Investigated a live user report ("boxes go uneven a few seconds after refresh, every time, desktop Chrome") — root cause was that real-world font/text metrics can settle later than all of the fixed correction points (double-rAF, 200ms timeout, `window.load`, `fonts.ready`), leaving the equalized height stale with nothing left to re-correct it. A `ResizeObserver` on the inner `.events` element (not the `.zone-events` wrapper, which carries our own forced `minHeight` and would create a feedback loop) reacts to any future natural-size change, indefinitely | The previous fix (this session's predecessor) added more fixed-timing passes (double-rAF, `setTimeout`, `load`, `fonts.ready`) — insufficient, still had a window where late reflows went uncaught |
| `applyFilterVisuals()` scoped to `#board .task-row[data-id]` | Selector was also matching task-pool rows outside `.day` elements, causing a null-dereference crash on `switchToBoard` | — |
| Testing rule: never test on real boards, always use a disposable scratch board created/deleted via direct API calls | A past incident: testing accidentally happened on the real board "לוז מרכזי - אוריה" and required manual repair | — |
| Delete → undo toast, not a confirm dialog | User's explicit call when both were offered: keeps fast deletes fast, only adds friction (a few seconds' reversibility window) rather than an extra click on every delete | A `showConfirmModal` step before every task/event delete — rejected as too much friction for a frequent action; people/topic deletion still uses confirm since it's rarer and higher-blast-radius (cascades to referencing tasks) |
| `--muted` darkened, checkbox border moved off the shared `--border-strong` onto `--muted` | Contrast audit found `--muted` failing AA (3.4–3.9:1) and the checkbox border failing the 3:1 UI-component minimum (1.57:1); scoping the checkbox fix to `--muted` (not darkening `--border-strong` itself) avoids darkening every decorative divider in the app, which shares that token | Darkening `--border-strong` globally — rejected, too large a visual footprint for a refinement pass, would have darkened dividers that don't need the higher contrast ratio |
| Checkbox keeps double-duty as drag handle; added a `title` tooltip instead of a separate visible handle icon | The checkbox-as-handle is a recent, deliberate, already-shipped decision (see above) — swapping in `dragHandleEl()` for task rows would mean re-wiring `setupDrag`'s target element, risking the working drag system for a P3 (discoverability, not a bug) | A visible `.drag-handle` dot on task rows matching event rows — rejected as touching drag logic for a low-severity, cosmetic-consistency issue |
| Split `weekend` into `fri`/`sat`, 7 columns everywhere | A month grid needs per-date resolution; a merged bucket cannot express it. Accepted cost: the week view changed too (the double-width סופ״ש card is gone), and **existing weekend items all landed on Friday** — which of the two days they belonged to was never stored and is not recoverable | A 6-column month grid (א–ה + סופ״ש) needing zero migration was recommended and explicitly rejected by the user in favour of a real 7-column calendar |
| Month view is read-only; editing happens in a day peek | At month density a cell is ~1/5 the height of a week card — sections, chips and add-lines don't fit. Drag targets of ~50×70px on touch are a mis-drop generator on live data with no per-item history. The peek reuses the existing row renderers **unchanged** by pointing `weekStart`/`weekData` at the clicked cell's week first (`setWeekContext`), so full editing cost no new interactive components and `setupDrag` was never touched | Full drag-and-drop in the month grid (Google Calendar / Notion style) — deferred, not dropped; drag from the task pool onto a day is the strongest future candidate |
| View mode in `localStorage` per user+board, not in `appState.settings` | `settings.showBoard`/`showSummary`/`sectionOrder` are deliberately board-wide and shared; view mode is a personal preference. In the shared blob, one person switching to month view would switch it for the whole staff on the next 20s poll | Storing it in the board blob alongside the other settings — rejected for the reason above |
| Chunked `State` columns over pruning or archiving old weeks | Keeps the "backend doesn't understand the shape" property and needs zero frontend change | Archiving old weeks to another tab (changes read semantics; `topicLastActiveDate` and the month summary both iterate all weeks) and client-side pruning (data loss by design) |
| Month-scoped summary rows carry their own `ws`, and the day chip calls `setWeekContext(ws)` before `moveTaskToDay` | `moveTaskToDay` operates on `weekData` — the *focused* week. That was safe while `collectItemsFor` only ever scanned the current week, but the month-scoped digest surfaces rows from four to six different weeks. Without this the move would splice the item out of a week that doesn't contain it and push it into the focused week, silently relocating a task by up to a month. Verified: moving a 13.09-week task tue→thu keeps it in the 13.09 week and leaves the focused week untouched | Making `moveTaskToDay` take an explicit week argument — equivalent, but `setWeekContext` is the pattern already established for "aim the globals at this row's week", so it stays consistent with how the day peek does it |
| Day title (`data.title`) surfaced in the month cell and editable from the day peek | A day title means "the whole day is X", which is exactly the kind of thing a month overview exists to show; hiding it there would make the month view misleading about how full a day is. The peek reuses `renderDayBanner` verbatim — it writes via `saveWeek(weekStart, weekData)`, which `setWeekContext` has already aimed at the right week | Showing it only as a chip alongside the item chips — rejected, it would sort below events and read as just another item rather than a whole-day statement. At mobile density the text is dropped for a solid accent bar, since 45px cannot carry it |
| `peekWeek()` for month rendering instead of `loadWeek()` | `loadWeek` inserts an empty week into `appState` for any week it's asked about, so browsing months would permanently add dozens of empty week objects to the saved blob — the exact growth the chunking work exists to contain | — |
| CSS transitions added for drag-ghost opacity, drop-zone highlight, and event-zone height correction; auto-scroll speed ramped via a numeric formula change | A dedicated motion review found all three snapping instantly (no `transition` anywhere in the relevant rules) — genuinely jarring/ambiguous during drag, per a live user-facing UX critique. Transitions had to go on the *base* row/container rules, not just the modifier class, since a class-removal transition needs the property present in both the before and after computed style | Re-architecting the drag system (floating drag-ghost following the cursor, FLIP-animated reordering) — explicitly out of scope; this was a "don't touch drag logic" constrained review, transitions/timing only |
| Day title rendered as a sibling of `.day-head`/`.zone-events` (not inside either), with its own `.day-banner` class | Standard calendar UX pattern (Google/Outlook/Apple: a distinct "all-day" area above the timed grid, not a slot). Keeping it out of `.zone-events` means `setupDrag`'s dropzone scan and the `.zone-events` `ResizeObserver`/height-equalization never see it — zero risk to either system | Putting the title inside `.day-head` as a second line — rejected, would force `.day-head` height to vary with title length and complicate the existing header layout; a dedicated sibling element is simpler and fully decoupled |
| Day title always `contentEditable` (mirrors event/task text), not gated behind an edit-pencil button (unlike board rename) | Consistent with how every other piece of day-card text is edited in this app; a pencil-button pattern exists only for the rarer, higher-friction board-rename action | A pencil-button edit affordance like board rename — rejected as unnecessary friction for a same-frequency-as-event-text field |
| Day assignment from summary/pool rows built as a dedicated small `openDayPicker` popover + `moveTaskToDay` data-move, not drag-and-drop into the day board | The summary section (`#summaryGrid`) and the day board (`#board`) are separate DOM subtrees/scroll regions — extending `setupDrag`'s dropzone system across them would be materially more complex and fragile than a click-based picker; `collectItemsFor` only ever scans the *currently viewed week*, so the picker only needs to offer that week's 6 days, no week/date picker needed | Making `renderSummaryRow`/`renderPoolRow` draggable into `#board`'s dropzones — rejected, cross-subtree drag-and-drop is high-complexity for a rarely-needed capability once a click-based picker exists |
| Day chip always clickable, including on tasks already assigned to a day (shown in a summary card) — can reassign or unassign, not just assign once | User's explicit call: full symmetry with how the person/topic chips already work (always clickable, even when already set) | A read-only day badge for already-assigned tasks, editable only when unassigned — rejected as an inconsistent, harder-to-explain half-measure |
| Newly day-assigned tasks always land in the day's "משימות" (general) section, never "צוות" (team) | User's explicit call: simple, predictable default; section can still be changed afterward by dragging within the day, same as any task | Prompting for a section choice at assignment time — rejected as unnecessary extra friction for a rare edge case |
| Google ID token verified **once at login**, exchanged for an opaque session token, not re-verified on every request | Verifying via `UrlFetchApp.fetch` to Google's tokeninfo endpoint on *every* poll (every 20s × every open tab) would risk the Apps Script UrlFetch daily quota and add real latency to every request; a 90-day session in a `Sessions` sheet tab + `CacheService` (6h TTL) is nearly free to check | Verifying the ID token on every request — rejected, quota and latency cost with no real security benefit for this threat model |
| `REQUIRE_AUTH` Script Property gates plain-`email` auth off, default `false` (grace period) | The org has a very small number of users (one unit + its staff); old cached tabs and the pre-upgrade client must keep working until everyone has signed in with Google at least once, without a hard cutover that breaks someone's open tab mid-task | A hard cutover the moment the new frontend deployed — rejected, no way to know in advance whether every open tab had already re-signed-in |
| `setAccess`/`removeAccess` refuse to leave a board with zero explicit `editor` rows (`last_editor` error) | Losing the last editor would make a board's `Config` un-editable through the app (no one left with permission to grant anyone else access) — the only recovery would be a manual Sheets edit | Allowing it and relying on manual Sheets recovery — rejected, an easy self-inflicted lockout for a guard this cheap to add |
| `setScriptConfig` API action (gated to board-1 editors) instead of requiring Apps Script editor access to set `GOOGLE_CLIENT_ID`/`REQUIRE_AUTH` | `clasp run` isn't wired up for this project (returned `NOT_FOUND` when tried) and container-bound scripts don't appear in script.google.com's project list, making "open the editor and run a function" a multi-step, account-sensitive process; an API action reachable by any real staff editor is simpler and repeatable | `clasp run` — tried first, didn't work in this environment; relying solely on manual Apps Script editor access — still documented as the fallback (see Watch Out), but not the primary path |
| `use_fedcm_for_button: true` on `google.accounts.id.initialize()` | Without it, the *rendered button* (not just One Tap) falls back to the legacy popup + `window.postMessage` flow, which current Chrome blocks by default ("Cross-Origin-Opener-Policy policy would block the window.postMessage call") — the credential callback then silently never fires. This was the root cause of a long "stuck on מתחבר" debugging session (see Watch Out) before it was traced to this one missing flag | Nothing — this is Google's own documented fix; the alternative (`ux_mode:'redirect'` with a server-rendered `login_uri`) doesn't fit a static-hosted, no-build frontend |
| `summarizeChange()` diffs the state client-side into a short note, sent as `saveBoard`'s `note` param, rather than having the backend diff prev/next state | The backend already treats the state blob as opaque by design (Decisions, above) — diffing it server-side would mean parsing/understanding the shape there for the first time, undermining that boundary. The client already has both the previous and next state in memory at save time | Backend-side diffing — rejected, breaks the "backend never interprets the blob" boundary that the whole schema-less design relies on |
| Notes board content restricted to `b/i/u/s/br`, sanitized on every commit **and** every load | The backend treats the whole blob as opaque (Decisions, above) and cannot validate content shape — a board's data can in principle be written by anything that reaches the API, not just this client. Re-sanitizing on load (not just on save) means a malicious or malformed write from elsewhere is neutralized the moment any client reads it back, not just prevented going forward | Sanitizing only on save (trusting whatever was already stored) — rejected, leaves a window where a bad write already in the sheet renders unsanitized until the next edit touches that block |
| Paste in the outline editor inserts plain text only (`e.preventDefault()` + `execCommand('insertText', ...)`), never sanitizes-and-reassigns pasted HTML | Reassigning `el.innerHTML` after sanitizing a paste risks losing the caret position, since the DOM subtree the caret was anchored to gets replaced — a whole class of bugs is avoided by never letting rich HTML into the contentEditable in the first place. The block only ever needs the a few inline styles this editor's own shortcuts/toolbar apply, which `sanitizeInline` already covers on commit regardless | Paste rich HTML then sanitize + reassign `innerHTML` — rejected as fragile (cursor-jump risk) for no real benefit, since Word/web paste formatting isn't part of this editor's scope anyway |
| Notes board structural editing re-renders the whole outline on every change (`commitNotesChange` → `renderNotesOutline`), not a minimal DOM patch of just the affected subtree | A first-version simplification: page sizes in realistic use (tens of blocks) make a full re-render unnoticeable, and it keeps every structural operation (split/indent/outdent/move/delete) short and obviously correct instead of hand-rolling a targeted patch for each one. Caret position is explicitly restored afterward (`focusBlock`) so editing still feels continuous | A minimal-diff re-render per operation — deferred, not rejected; worth revisiting if a page ever grows large enough for the full re-render to be visibly slow |
| Notes collapse state (`Block.collapsed`) stored in the shared blob, not per-user `localStorage` like `makeCollapsible` | A collapsed/expanded state is content the gallery preview needs to reflect for everyone (Notion-style: what's collapsed is part of how the page reads), not a personal viewing preference — the opposite reasoning from why month/week view mode is deliberately per-user (Decisions, above) | Per-user localStorage like `makeCollapsible` — rejected, would make the gallery preview show a different outline shape per viewer, which defeats the point of a shared preview |
| PWA manifest with no service worker | This is a live-data app on a 20s poll — a SW's main benefits (offline shell, background sync) don't apply, and its main risk (a cached `index.html` silently serving stale app code to staff after a deploy, with no natural cache-bust since there's no build hash in the filename) is a real cost for zero benefit here. The icon/manifest goal ("designed icon on the home screen") needs no SW at all | Adding a minimal SW anyway for PWA-installability signals (e.g. Chrome's `beforeinstallprompt`) — rejected; iOS Safari's "Add to Home Screen" (the actual target platform for this org) doesn't require a SW, only a valid manifest + `apple-touch-icon` |
| `apple-touch-icon.png` rasterized from the full-bleed (no radius) SVG, not the rounded-corner one | iOS applies its own corner-rounding mask to the touch icon; shipping a pre-rounded PNG would double the effect (two different radii layered), producing a visibly wrong/mismatched corner | Using the same rounded PNG as the favicon/manifest icons — rejected once cross-checked against Apple's own guidance (touch icons should be full-bleed square) |
| **Storage: stay on Google Sheets via Apps Script, not migrate** (2026-09-16 research, run in parallel with the Google sign-in work) | At current + projected growth (Google sign-in's `Sessions`/`Log` tabs, and a future `notes` board type estimated at 25k–200k chars/board) the real binding constraint is **read amplification and poll cost**, not the 540k chunk ceiling — `getBoardState`/`getBoardsTable` currently re-read every board's full state on every single-board request, and the 20s full-blob poll gets expensive well before any size limit is hit. Those are fixable in the existing architecture for ~2–4 person-days (see Watch Out #13 and the hardening list there) and keep the Sheet the org can open and the maintainer can hand-repair, and the Apps Script access model the sign-in work is already built on. Full report: [`docs/storage-recommendation-2026-09-16.md`](docs/storage-recommendation-2026-09-16.md) | Firebase Firestore — real-time listeners would remove the poll entirely and its 1 MiB/doc limit forces the same per-page split as staying on Sheets, but it drops the inspectable Sheet and the existing Apps-Script-based access model for a rewrite (6–12 days); rejected as disproportionate to the actual problem. Apps Script + Drive JSON files — the natural next step **if** a single board ever exceeds ~300k chars, not before. Supabase — free-tier projects pause after ~1 week idle, a real risk for bursty education-staff usage; rejected outright. Cloudflare Workers+KV — KV's 1,000 writes/day free quota is below a normal editing day; rejected. |

## Deploy ordering for the current change (do not reorder)

The backend chunking + the frontend's save-failure handling **must be live before** the fri/sat migration reaches users — otherwise boards migrate through a save path that still reports success when the server refuses the write.

1. `clasp push` → `clasp version` → `clasp deploy --deploymentId AKfycbx…UgCdwVXCkUU --versionNumber <new>` (reuse that deploymentId).
2. Verify on a scratch board: a >50,000-char state round-trips across `State`/`State2`, and an over-capacity state returns `state_too_large` and shows a visible failure rather than `מסונכרן ✓`.
3. Only then push the frontend.

## In Progress / Next Up

**Phase-3 remediation in progress (2026-09-17).** Executing `frontend/.design/PLAN-2026-09-17.md` (from the audit `frontend/.design/AUDIT-2026-09-17.md`), batch by batch; progress log in `frontend/.design/EXECUTION-LOG-2026-09-17.md`.

**Shipped 2026-09-17 — Batch 2 (data-integrity & races), backend v19 + frontend.** Backend: `getBoardState` reads under the save lock and returns a `state_corrupt` signal on a parse failure instead of a silent empty board (so the client keeps its copy rather than saving empty over real data); delete/rename/removeAccess row mutations now hold the script lock (`withScriptLock`). Frontend: `refreshFromServer`/`pushState` capture the board id before each `await` and skip stale writes (no cross-board clobber on a switch mid-request); `switchToBoard` stays put on a failed flush; logout and tab-close flush a pending edit only when there is one; `sanitizeNotesState` backfills missing `blocks`/`children` arrays (a malformed write can't crash a page); deleting a person/topic clears dangling refs; the notes editor intercepts drag-drop to insert plain text only. Verified: v19 live scratch-board round-trip + concurrent delete/save; standalone logic/DOM proofs for the notes + ref-clearing fixes; clean live boot.

**Shipped 2026-09-17 — backend v18 (Batch 1, B1.1 + B1.2).** Two security fixes:
- **Closed the grace-period privilege backdoor** (audit #2): `setScriptConfig`/`setAccess`/`removeAccess`/`setDefaultRole` now reject any non-Google (`auth !== 'google'`) caller with `auth_required` and log a `plain_denied` row, via a new `isGoogleAuth(auth)` guard placed *before* the role check — so a plain-`email` caller can no longer pass a real editor's address and change access or the Google client id during the grace period (a change that would have survived the eventual `REQUIRE_AUTH` flip). Bootstrapping is unaffected (`GOOGLE_CLIENT_ID` already set; `adminSet*` editor fallbacks still work).
- **Spreadsheet formula-injection guard** (audit #5): a new `safeCell(v)` prefixes a literal apostrophe on any user-controlled string starting with `= + - @` (tab/CR) before it's written to a cell, applied centrally in `appendRowByHeader` (covers Boards/Config/Sessions/Log appends, incl. `logEvent` Details/Board) and at the direct `renameBoardRow` `setValue`. Emails are format-validated instead (`isValidEmail`, must start alphanumeric, so a valid email is never prefixed and access-matching is unaffected); board names are length-capped/newline-stripped (`cleanBoardName`, 80 chars). Verified live on a scratch board: an injected `=IMAGE("http://evil/x")` board name round-trips back as **inert literal text** (not an evaluated formula); plain-auth `setDefaultRole`/`setScriptConfig`/`setAccess` all return `auth_required`; normal plain-auth `saveBoard`/`createBoard` still work (grace period otherwise intact). Scratch board deleted; `listBoards` clean.
- **Still open in Batch 1:** remaining frontend hardening items, then the operational `Log` audit → flip `REQUIRE_AUTH` (gated behind B1.1 being live [done] + a clean Log audit). Details in the local (git-ignored) plan/execution-log, not tracked here.

**Shipped 2026-09-17.** Notes-board terminology + first Notion-inspired polish pass. Renamed the board type and its entries from "דפי הערות"/"דף" to **"לוח כתיבה"/"רשומה"** everywhere in the UI (modal card, board-switcher icon tooltip, gallery header/button/empty-state, delete confirmations) — the old wording read as "pages," which the user felt was inaccurate for what's actually a nested-outline document. The new-board card's description also dropped the "כמו ב-Notion" namedrop. Then, per an approved short design proposal (2 of 5 items, the rest deferred pending user priority): the "רשומה חדשה" button shrank from a large solid button to a small outlined pill (no dropdown chevron — unlike Notion's "New ▾", there's only one entry type here, so a chevron would imply a menu that doesn't exist); gallery cards now lift with a soft shadow on hover and their "⋯" menu only appears on hover (always visible on touch — `pointer:coarse`, same pattern as `.row-del`/`.drag-handle`). The "⋯" menu (previously delete-only, in both the gallery card and the in-page toolbar) is now a real dropdown with **שכפול/שינוי שם/מחיקה** (duplicate/rename/delete), reusing the existing `.picker` popover component that backs the highlight-filter menu — no new UI primitive. Rename opens the page and selects the title text (same contentEditable field as clicking in manually) rather than a separate prompt/modal. The in-page menu button, which had no role check before, now also hides for viewers.

**Shipped 2026-09-17.** Notes-board polish pass, items 3-5 of the approved Notion-inspired list (1-2 shipped separately just before this): a **breadcrumb** ("כל הרשומות › <כותרת>", live-updated on every title edit) replaces the old plain back button; an **optional per-page icon/emoji** — a small `contentEditable` square next to the title, click selects its whole content so typing/pasting an emoji replaces it in one step (same interaction pattern as rename) — stored as `page.icon`, sanitized to 8 chars via `sanitizeNotesIcon` (defensive cap on a giant paste, not a security boundary — this field is never rendered as HTML), carried through `duplicateNotesPage`, shown next to the title on the gallery card too when set; and a **typography pass** (page title 26px→32px, `ob-h1` 21→24, `ob-h2` 17→19, body text 14.5px→15.5px, more margin above headings and more top padding in the page view) aimed at feeling like a document rather than a form. `sanitizeNotesState` now also sanitizes `page.icon` on load, matching the existing defensive-resanitization treatment of block `html`.

**Shipped 2026-09-17.** Three bugs reported live during today's work, found and fixed same-session:
- **`createBoard`/every `saveBoard` was slow** — `ensureColumn` (called by `createBoardRow` on every board creation and by `saveBoardStateRaw` for `StateHash` on every save) read the *entire* Boards sheet via `getDataRange()` just to check whether one header cell existed — the exact read-amplification pattern the 2026-09-16 storage hardening fixed elsewhere but missed here. Backend v17. Verified live: 3 `createBoard` calls against the live backend after the fix, 4-11s each (normal Apps Script exec latency), no more multi-tens-of-seconds hangs.
- **The board switcher (drawer) would go empty on refresh** — `fetchBoardsList()` returned `[]` on *any* failure (a transient non-JSON interstitial, Watch Out #18, not just "genuinely zero boards"), and every call site — most importantly the 20s poll in `startPolling` — unconditionally overwrote `boardsList` with that empty result. Now returns `null` on failure; a new `refreshBoardsList()` wrapper only overwrites `boardsList` on an actual success, so a transient hiccup no longer blanks the list. `deleteBoard`'s call site locally removes the deleted board on a refetch failure instead of trusting a stale (still-includes-the-deleted-board) list.
- **The new block-type dropdown opened with a large empty gap above the toolbar** — `positionPopover`'s "flip above the anchor" branch used a fixed 280px assumed height (matching `.picker`'s CSS `max-height`) instead of the menu's real height, so a short menu (e.g. this 5-item, ~165px one) got positioned as if it were much taller. Fixed by measuring the popover's actual rendered height (`pop.offsetHeight`) — required moving the `positionPopover(...)` call to *after* `document.body.appendChild(pop)` in all 6 popover call sites (highlight filter, summary mode, day picker, person/topic picker, block-type menu, notes page menu), not just the new one. Verified: measured gap dropped from ~150px to the intended 6px against a real anchor in the live page; spot-checked the unrelated "position below" branch (`openHighlightMenu`) still renders correctly.

**Shipped 2026-09-17.** Two follow-up reports on the same day, after the user found the earlier fixes above didn't fully resolve what she was seeing:
- **Switching boards still felt slow** — a separate cause from the `ensureColumn` fix: `switchToBoard()` unconditionally `await`ed a full `pushState()` (a `saveBoard` POST round-trip to Apps Script) before entering the new board, on *every* switch, even when nothing had actually changed since the last save. Now gated on `hasPendingLocalChange`, so a plain "look at another board" switch no longer pays for a network round-trip it didn't need.
- **The board list was still going empty on refresh** — the earlier `fetchBoardsList`/`refreshBoardsList` fix stops a transient failure from *overwriting* an existing list, but a page refresh starts with no list in memory at all, so a single slow/failed `listBoards` call on boot had nothing to fall back on and nothing to retry it — the drawer stayed on "עדיין אין לכם לוחות" for the whole page view. `refreshBoardsList()` now retries twice (1s, then 2.5s backoff) specifically when `boardsList` is still empty going in; once a real list has loaded, the existing "keep the stale list on failure" behavior is unchanged.

**Orphaned scratch board note**: a `createBoard` call during this session's own perf verification returned a client-side `auth_required` (the well-known Watch Out #16/#18 pattern — the POST had actually already succeeded server-side) and left `board-65a8f9d1` behind; found and deleted during cleanup. Also noticed the account's old `board-4ff4cccf` ("גאיה", planner type) no longer appears in `listBoards` and `getBoardMeta` returns `not_found` for it — genuinely gone from the `Boards` sheet, not an access/role issue (confirmed via `getBoardMeta`, which checks board existence before role). No code path introduced this session calls `deleteBoard` against it, and a same-session new notes board ("סיכומי פ.ע") suggests this may well be the user's own deliberate cleanup/creation rather than a bug — asked the user directly rather than assuming either way.

**Shipped 2026-09-17.** Notes editor: visible desktop formatting toolbar + numbered/checkbox lists (closes Watch Out #29's flagged formatting-UI gap, and the user's follow-up request to add new list types). `.ob-toolbar` is no longer `pointer:coarse`-only — on a mouse it now renders as a small floating bar (centered, max-width 520px, smaller buttons) instead of staying hidden; the old blind "H" cycle-button became a real dropdown (`openBlockTypeMenu`, reuses `.picker`) listing all 5 block types with the current one highlighted. Two new block types: `ol` (auto-numbered — `computeOlIndex`/`renderBlockList` track a running count per sibling array, resetting on any non-`ol` block, same rule as Markdown) and `todo` (`block.checked`, strikethrough via `.ob-row.is-checked`, `toggleBlockChecked`). Markdown autoformat extended (`"1. "` → numbered, `"[] "`/`"[ ] "` → checkbox) alongside the existing `"# "`/`"## "`; new shortcuts `mod+shift+7`/`9` match Notion's own numbered-list/to-do bindings. Split style-setting into two paths: `setBlockStyleInPlace` (mid-typing markdown autoformat — patches the text class + marker sibling without a full re-render, so the caret is never disturbed) vs. `setBlockStyleAndRefresh` (keyboard shortcuts + the dropdown — full commit+re-render+refocus, so numbered-list renumbering of later siblings is always correct). Gallery card previews, `duplicateNotesPage`, and `sanitizeNotesState` all carry the new fields through. **Verified via injected markup matching each render function's real output** (same technique as the rest of today's notes-board work — a real signed-in session can't be scripted here, FedCM) at desktop and 375px; the real keyboard-shortcut/`execCommand` code paths were reviewed carefully but not exercised interactively — worth a manual pass once, same caveat as the PWA icon.

**Shipped 2026-09-17.** Storage hardening (2026-09-16 research, Watch Out #25 items 1–3, the highest value/effort items on the list). Deployed as backend **v15** on the existing deploymentId:
- **Read-amplification fix** (items 1+2): `getBoardState` and `getBoardsTable` previously called `sheet.getDataRange().getValues()`, pulling every board's full `State`/`State2`../`StateN` chunks (up to ~540,000 chars each) on every single-board `getBoard` and every `listBoards` call, even though `listBoards` never reads `.State` at all. Both now do a targeted read: a one-row header read to locate columns by name (never by position — verified this matters, since `Type`/`StateHash` get appended wherever `ensureColumn` finds the header at the time, which can be *after* the State columns), a single-column `BoardId` scan to find the row, then only that one row's cells. A new `readTableColumnsOnly` helper (alongside the existing `readTableByHeader`, still used for the small Config/Sessions/Log sheets) implements the pattern generically.
- **Hash-based poll** (item 3): a new `StateHash` column (MD5 of the JSON, `computeStateHash`) is written on every `saveBoardStateRaw`. A new `getBoardMeta` GET action returns just `{ok, role, board, hash}` — no board data. The frontend's 20s poll (`refreshFromServer`) now calls `getBoardMeta` first via a new `boardUnchangedSinceLastSync()` helper and skips the full `getBoard` fetch when the hash matches `lastKnownHash`; a `getBoard` response also returns `hash` so `lastKnownHash` stays current after a real fetch. An empty/missing hash on either side (a board never saved since the column was added, a network hiccup, an auth error) always falls through to the full fetch — the optimization is fail-safe by construction, it can only ever skip *unnecessary* work, never skip a real change.
- Items 4 (verify the Google ID token only at login, never on the poll path) and 5 (batch/sample `Log` appends per save) from the hardening list were **not** touched this round — #4 was already true of the existing code (nothing to change), #5 is lower value/effort and deferred.
- **Verified**: a Node-based mock of the Sheets API (`SpreadsheetApp`/`Utilities`/`LockService`, no real Apps Script project touched) exercised `getBoardsTable`/`getBoardState`/`saveBoardStateRaw`/`getBoardMetaForClient` against a synthetic sheet with `Type` deliberately positioned *after* `State` (the exact column-drift scenario a position-based read would get wrong) — 20 assertions, all passing, including multi-chunk round-trip, cross-row isolation, hash stability across identical resaves, hash change on real content change, and no stale trailing chunk after a shrinking save. Then against the **live** backend on disposable scratch boards (created and deleted via the API, never the two real boards): single- and multi-chunk (60,228-char, 2-chunk) planner and notes boards round-tripped byte-identically; `getBoardMeta`'s hash tracked every save exactly (unchanged on an identical resave, changed on real content changes); `getBoardMeta` confirmed reachable and CORS-clean from a real browser console. `listBoards` after cleanup showed only the two real boards (`board-1`, `board-4ff4cccf`) — confirmed untouched throughout. **Not verified live end-to-end through the actual sign-in UI** — Watch Out #20 (FedCM can't be automated) blocked getting a real session in the local preview again; relied instead on the backend's own thorough live verification plus the fail-safe design (any error/ambiguity on the frontend side falls through to the old always-fetch behavior, so a bug here could at worst cost the perf win, not correctness).

**Shipped 2026-09-16/17.** Google sign-in, per-board access management, and an activity log (see Current State/Architecture/Decisions above for what shipped). Backend deployed incrementally as **v11 → v14** on the existing deploymentId as each piece was verified:
- **v11**: sessions, `login`/`logout`, `resolveIdentity`, access-management actions (`listAccess`/`setAccess`/`removeAccess`/`setDefaultRole`), `getActivity`, `Boards.Type` column. Verified against a scratch board (created and deleted via the API): bad/missing token → `bad_token`, expired/bogus session → `session_expired`, plain email still works pre-`REQUIRE_AUTH`, access add/remove/last-editor-guard round-trip, activity log shows the right rows, notes-type board creation round-trips, bad type falls back to `planner`. Also confirmed Watch Out #17 was stale (create→delete by the same email already worked).
- **v12**: added `setScriptConfig` (board-1-editor-gated) after `clasp run` turned out not to be wired up for this project (Watch Out #24) — used it to set the real `GOOGLE_CLIENT_ID` once the user created it in Google Cloud Console.
- **v13**: fixed a real bug found while debugging live sign-in — `getActivity`'s board-less-row filter only special-cased `'login'`, so `login_failed` rows (and `setScriptConfig`) were invisible in the drawer's activity view even though they were being logged correctly (Watch Out #22).
- **v14**: added a server-only `debug` field to `verifyGoogleIdToken`'s failure paths, logged (never sent to the client) via `login_failed`'s `Details` column — this is what finally surfaced the real root cause below, after every prior symptom looked identical from the outside.

**The actual live-sign-in debugging trail**, kept because the failure mode was genuinely confusing and is likely to recur on the next external-API addition: every real Google sign-in attempt looked like it hung forever ("מתחבר…" with nothing ever resolving) or, once `use_fedcm_for_button:true` fixed a real separate bug (the button was using the legacy popup+`postMessage` flow, which current Chrome blocks by default — Watch Out entry in Decisions), failed with a generic "ההתחברות נכשלה". The v14 debug field revealed the true cause: `UrlFetchApp.fetch()` (added this session, for `verifyGoogleIdToken`'s call to Google's tokeninfo endpoint) had never been authorized for the account the web app executes as — `clasp push`/`clasp deploy` don't trigger Apps Script's OAuth consent screen for a newly-required scope, only a human running a function through the editor UI does. See Watch Out #19 for the fix and #20/#21 for two false leads chased before finding it (FedCM can't be automated; Claude in Chrome's own extension messaging can throw a similar-looking but unrelated error).

Also, mid-verification, a local test session's default board resolution (no `?board=` param) landed a save on the real `board-1` — see Watch Out #23. Verified no data loss (11 weeks/6 people/6 topics/23,714 chars intact, normal growth) but it's now a documented near-miss, not just a rule.

Verified locally (both the in-app preview pane — which cannot complete FedCM, see #20, so only error-path testing there — and, for the actual sign-in, Safari and Chrome via a real Google account) and against the live backend on a scratch board (created/deleted): sign-in card + every error state (not-configured, GIS-blocked, bad-token, network), migration note for pre-upgrade browsers, access add/change-role/remove/default-role with real UI interaction, activity log rendering real log rows, mobile drawer layout at 375px. Frontend pushed to `main` after backend verification per the deploy-ordering rule.

Open follow-ups: flip `REQUIRE_AUTH` once rollout is confirmed complete (see Open Questions); the PWA manifest/icon (item 3) is still to build; the Sheets-vs-alternative storage research (item 4) ran in parallel this session and is folded in — see the "Storage: stay on Google Sheets" Decisions row and Watch Out #25.

**Shipped 2026-09-17.** PWA manifest + home-screen icon (item 3 of the original ask). Icon designed and rasterized (source `frontend/icons/icon.svg`/`icon-maskable.svg`), `manifest.webmanifest` added, `<head>` wired up (manifest link, favicon, apple-touch-icon, theme-color + apple/mobile-web-app meta tags). No backend involved. Verified: all resources (manifest + 4 icon files) confirmed serving 200 with correct content-types both locally and on the live GitHub Pages URL (there was a brief window right after push where the brand-new paths 404'd while GitHub Pages' CDN finished propagating them — confirmed via `gh api .../pages/builds/latest` that the build itself had already succeeded with no errors; this was propagation lag, not a real failure, and resolved within under a minute). **Not verified**: the literal "tap Add to Home Screen and see the icon" on an actual device — this machine has only Xcode command-line tools, not a full Xcode install, so the iOS Simulator tool refused to attach. Left as an open manual check (see Watch Out) rather than silently assumed to work.

**Shipped 2026-09-17.** Notes board type (item 2 of the original ask) — see Current State/Architecture/Decisions above for what shipped. Backend Type-column support (`ensureColumn`, `normalizeBoardType`, `defaultBoardState(type)`, `createBoard(email,name,type,auth)`) was actually written and deployed as part of the same v11 push as Google sign-in, ahead of the plan's original staging — it was already live and verified (planner + notes scratch boards, round-trip, bad-type fallback to `planner`) before this session's frontend work started, so no separate backend deploy was needed for this stage. Frontend: new-board modal type choice, `currentBoardType`/`isValidStateFor`/`defaultStateFor` plumbing through every state-load site, the notes gallery, and the full nested outline editor with keyboard shortcuts + touch toolbar.

Verified: a realistic nested Hebrew blob (heading + nested bullet, inline bold) round-trips byte-identically through the live backend on a disposable scratch board; gallery preview marks (▶/▼/•) and depth match the live outline exactly; Enter/Tab/Shift+Tab/`# `-heading-shortcut/delete-with-undo all confirmed working via real keyboard interaction against a real Google-authenticated session (Chrome, via Claude in Chrome — see Watch Out #26 for a real caveat hit during this); `sanitizeInline` confirmed to strip `<script>`/`onerror`/`onclick`/`javascript:` from a blob written directly via the API (simulating a non-client write) while preserving `<b>` formatting. Scratch board deleted after. Frontend pushed; live-cache verification pending GitHub Pages' ~10-minute CDN window at the time of writing (Watch Out — re-verify `?cb=` on the live URL before considering this fully closed).

**Shipped 2026-09-15.** Fixed two independent root causes of a recurring "an added item sometimes disappears" report (separate from, and downstream of, the 2026-09-09 chunking fix below):

- **Backend race**: `saveBoardStateRaw` wrote chunked state via multiple unlocked `setValue()` calls — two concurrent saves to the same board could interleave and corrupt the concatenated JSON, which `getBoardState` then silently turned into an empty board on the next read (no log, no error). Fixed by wrapping the whole read-modify-write body in `LockService.getScriptLock()` (10s wait, released in `finally`); a new `'locked'` outcome surfaces to the client if the wait times out. `getBoardState`'s `JSON.parse` catch now logs via `Logger.log` (still returns `defaultBoardState()` — behavior unchanged, just diagnosable). Deployed as **version 10** on the existing deploymentId.
- **Frontend poll-clobber**: `refreshFromServer`'s full `appState = serverData` overwrite was gated only by `isUserEditing()` (a focus heuristic), which doesn't cover an edit still sitting in the 600ms `scheduleSave` debounce or in flight to the server (checkbox toggles, drags, deletes all lose DOM focus immediately). Added `hasPendingLocalChange` — set the moment a save is queued, cleared only on a confirmed successful `pushState()` — and `refreshFromServer` now also checks `!hasPendingLocalChange` before overwriting. New `locked` entry added to `SAVE_ERROR_TEXT`.

Both verified against a disposable scratch board (deleted afterward): the backend lock was confirmed by firing repeated concurrent-save pairs and checking for corruption (none, across multiple paced runs — an earlier unpaced run showed apparent "corruption" that turned out to be a non-JSON interstitial response from Google's infra under rapid scripted traffic, not real data corruption); the frontend guard was confirmed live in a browser against the real deployed backend — a local edit made mid-debounce survived a poll landing while another "device" (a direct API call) pushed a conflicting change to the same board, then saved correctly once the debounce completed.

**Shipped 2026-09-09.** Backend deployed as version 9 on the existing deploymentId (URL unchanged), verified on a disposable scratch board — a 124,380-char state round-trips byte-identically across 3 chunk columns, an over-ceiling state is refused with `state_too_large` and leaves the previous state undamaged, and the scratch board was deleted afterwards. Frontend pushed after that.

The old-client round trip was verified against the **real** previous `index.html` (served from `git show HEAD~1`) sharing one origin's localStorage with the new one, not just against a replica: the old client renders an empty סופ״ש column, edits and saves without touching `fri`/`sat`, and items it adds to `weekend` are drained into Friday exactly once by the new client.

Merged with the day-title-banner and day-assignment work that landed on `main` in parallel. Two integration points needed real work rather than a mechanical merge: `moveTaskToDay` had to become week-aware (see Decisions — it would otherwise have relocated tasks by up to a month once the summary spans a month), and the day title is now shown in month cells and editable from the peek. The day picker picked up the 7-day split for free.

Open follow-ups: remove the `weekend` sentinel in ~2 weeks (Watch Out #11); watch board growth (#14). Known limitation: assigning a **pool** task to a day from month view still targets the focused week (`weekStart`), which in month view is whichever week was peeked last — dated rows are unambiguous, pool rows are not. Worth a date-bearing day picker if it comes up.

## Open Questions

**When to flip `REQUIRE_AUTH` to `'true'`** (closes the grace period, refuses the old plain-`email` auth entirely — see Decisions). Wait until the `Log` tab shows no `Auth:'plain'` rows for a few days, i.e. every real user has signed in with Google at least once. With this org's very small user count that could be almost immediately — check the drawer's "פעילות אחרונה" or query `getActivity`. Flip with `adminSetRequireAuth(true)` in the Apps Script editor (Watch Out #19 for how to reach it) — no redeploy needed.

## Watch Out

1. **Never test/experiment on real boards.** Always: create a disposable scratch board via a direct `action:'createBoard'` API call, test on it, **delete it when done** (`action:'deleteBoard'`). Login defaults to the last-loaded board, which is sometimes a real one — always confirm the board name in the header/title before touching anything, and navigate via the board-management drawer or `?board=<id>` URL param if needed.
2. **Code deploys (frontend or backend) never touch saved data** — deploy only changes what the code *does* from that point forward. The only risk is manual API calls made while debugging.
3. Git: commit+push at the end of each successful step without waiting for approval — but always ask before merging any PR to `main` (not currently relevant, work happens directly on `main`).
4. Cosmetic/visual changes should go through Plan Mode first (plan → clarify → execute).
5. **Local browser testing**: files outside the session's own project folder (frontend lives in a different folder than the backend, which is the usual cwd) load as a static snapshot with no JS execution when opened directly. Working fix: create a temporary `.claude/launch.json` in the backend folder (the cwd) running `python3 -m http.server --directory <frontend path>`, `preview_start` with that name, then test at `http://localhost:<port>`. **Delete the temporary `.claude/launch.json` when done.** Also: the in-app preview browser can silently serve a *cached* copy of a local page across `navigate()` calls — if the dev server logs don't show a fresh request, force a real refetch with a cache-busting query string (e.g. `?cb=2`).
6. **Line numbers drift** — always re-verify with `grep -n` before editing, don't trust remembered line numbers (including the ones in this file — they were accurate as of this session but will drift).
7. `/Users/createuser/Desktop/מערכת לוז ומשימות - גב/task-platform-handoff.md` is a **stale, outdated handoff doc** from a very early session (describes the project as pre-code, "Hosted vs. Local" undecided, etc.) — already marked with a banner at the top pointing here; this NOTES.md file supersedes it. Don't treat its content as current.
8. The backend folder (`מערכת לוז ומשימות - גב`) is **not a git repo** — only the frontend is version-controlled. Backend changes are tracked only via `clasp version` history in Apps Script itself.
9. Automated/headless browser testing environments (e.g. the in-session preview browser) can suspend `requestAnimationFrame`/`ResizeObserver` callbacks until a real paint is forced (e.g. by taking a screenshot) — don't conclude a rAF/RO-based fix is broken just because it doesn't fire immediately in that environment; force a paint and recheck before deciding.
10. Synthetic `PointerEvent`s dispatched from `javascript_tool` in the preview browser only reach the app's `setupDrag` listeners *after* that same rAF-suspension (point 9) has been cleared by a forced paint — a synthetic drag test that shows no reaction isn't proof the drag code is broken, it's likely proof no paint happened yet. Screenshot first, then dispatch.
11. **The `weekend` compatibility sentinel is temporary — remove it ~2 weeks after the fri/sat rollout.** `loadWeek` writes `data.weekend = emptyDayData()` on every week it touches. This is load-bearing during rollout: an old cached client folds `fri`/`sat` back together *only when `weekend` is absent*, so removing the sentinel early would let stale tabs re-merge and re-save days they can no longer represent. It must stay an **object** — an empty array is also truthy but makes the old `renderDay` call `data.events.forEach` on an array and crash the board. Verified by test: old→new round trips preserve `fri`/`sat`, and items a stale tab adds to `weekend` are drained into Friday exactly once.
12. **Weeks are still never pruned.** Chunking raised the ceiling to ~540k characters, it did not remove it. If a board ever approaches that, the next step is archiving or pruning old weeks — not more chunk columns.
13. ~~Poll cost is unaddressed~~ — **fixed 2026-09-17, backend v15.** `getBoardMeta` + `lastKnownHash` now skip the full `getBoard` fetch on an unchanged poll; `getBoardState`/`getBoardsTable` no longer re-read every board's full state on every request either. See the "Shipped 2026-09-17. Storage hardening" entry in In Progress and Watch Out #25 (items 1-3 done, 4-5 not).
14. **Board sizes, measured 2026-09-09** (read-only `getBoard`): `לוז מרכזי - אוריה` = **21,342 chars over 11 weeks — 43% of the old 50,000-char single-cell limit**; `גאיה` = 796 chars over 2 weeks. Re-measured 2026-09-16: `לוז מרכזי - אוריה` = **23,714 chars over 11 weeks** — normal organic growth (~340 chars/week over the intervening week), not truncation. At the original observed rate the main board would have hit the old ceiling at roughly 25 weeks of use, and (before the 2026-09-09 fix) would have failed **silently**. Re-measure occasionally; #12/#13 become live questions again well before 540k.
15. **`clasp`/API gotcha — a POST to `/exec` answers 302 to an echo URL.** Following that redirect automatically (`fetch(..., {redirect:'follow'})`, `curl -L`) downgrades the request to a GET on `/exec`, which hits the **legacy no-action read path** and returns a misleading answer such as `no_access` — while the POST itself has already been applied. This cost a real orphaned scratch board during deploy verification: `createBoard` reported failure but had actually created the board. Always follow the redirect **manually** (`redirect:'manual'`, then fetch the `location`) in any script that talks to this API, and re-list boards afterwards to confirm what really happened. The scratch-board helper script (`api.mjs`, rebuilt each session in the scratchpad) already does this — see #21.
16. A full `/impeccable critique`/`audit` + motion-review pass ran this session (scoped to the board/day view, task pool, picker, drawer, and the drag/height-equalization motion) — see git log on `main` for the three resulting commits (delete/contrast/visibility fixes, touch-target/checkbox/contrast fixes, then the motion-transition pass). All findings from that pass are resolved; a fresh pass would need to re-scope to whatever's changed since.
17. ~~`createBoard` grants no `Config` row to the creator~~ — **superseded, verified false as of v10+ (2026-09-16).** `createBoard` (קוד.js) does call `addConfigRow(boardId, email, 'editor')`; a scratch board's creator can `deleteBoard` it back out through the API with no manual Sheets edit needed. Either this was fixed between whenever this note was written and v10, or the original observation was itself mistaken — either way, don't budget for manual cleanup on this account anymore. (The specific orphaned board named in the old version of this note, `board-16a3f36e`, was not found in a 2026-09-16 `listBoards` — already gone.)
18. **`/exec` occasionally answers with an HTML interstitial instead of JSON** (`window['ppConfig'] = {productName: ...}` page) under rapid back-to-back scripted requests (seen repeatedly during the 2026-09-15 lock/poll-clobber verification, both on POST and GET). Cause not confirmed — looked like abuse/rate-limiting on Google's infra in front of the script, not an app bug (the very next retry, 1-2s later, normally succeeds). Any verification script hitting this API directly should treat a non-JSON response as transient noise and retry with backoff, not as proof of a real failure — an early pass of this session's own verification script mistook a burst of these for actual state corruption before adding retries. **A variant seen 2026-09-17**: under a rapid burst of scratch-board `saveBoard`/`deleteBoard` calls, a well-formed JSON `{"ok":false,"error":"auth_required"}` or `{"ok":false,"error":"forbidden"}` came back for a call that had worked moments before (same email, same board, no real access change) and then succeeded again on a plain retry with no code change in between. Unlike the interstitial case this *is* valid JSON, so a naive script won't retry it automatically — treat an isolated `auth_required`/`forbidden` under heavy back-to-back scripted traffic as suspect too, and confirm with a clean retry before concluding there's a real access/auth bug.
19. **A newly-added `UrlFetchApp` call needs a one-time manual re-authorization — `clasp push`/`clasp deploy` do NOT grant it.** Adding `verifyGoogleIdToken`'s call to Google's tokeninfo endpoint required the `script.external_request` OAuth scope, which Apps Script only grants when a human runs a function through the editor UI and clicks through the consent screen — clasp has no way to trigger this. Without it, every live call that reaches `UrlFetchApp.fetch()` throws `אין לך הרשאה להתקשר אל UrlFetchApp.fetch...`, caught by whatever `try/catch` is nearest and often surfacing as a generic, misleading error (here: every login looked like a plain `bad_token` rejection for hours — see In Progress for the full debugging trail). **Fix**: open `https://script.google.com/d/<scriptId>/edit` as the account the web app is deployed to execute as, pick a function from the Run dropdown that actually calls the new API (`doGet`/`doPost` won't do — their own top-level `try/catch` swallows the permission error before Apps Script ever shows the consent screen; a small dedicated function like `authorizeExternalFetch()` that unconditionally calls `UrlFetchApp.fetch(...)` works reliably), Run it, and approve the consent screen once. No redeploy needed afterward — the live `/exec` traffic inherits the grant immediately (`executeAs: USER_DEPLOYING`). **Do this again any time a new Advanced Service or external-facing API (Sheets API, Drive API, another external fetch target, etc.) is added to the script.**
20. **Google's FedCM sign-in flow cannot be driven by browser automation — not a bug, by design.** Neither the in-session preview browser (blocks the flow entirely with `FedCM get() rejects with NetworkError` — FedCM refuses to run inside an embedded/iframe-like context, the same restriction that applies to Chrome-extension popups) nor Claude in Chrome (can click the page's own GIS button, but the native account-chooser bubble that appears afterward is out-of-DOM browser chrome that synthetic clicks cannot reach) can complete an actual sign-in. **Any test that needs a real signed-in session requires an actual human clicking through it** — plan verification steps accordingly; don't spend time trying to script around this.
21. **Claude in Chrome's own content script can itself throw `"A listener indicated an asynchronous response by returning true, but the message channel closed before a response was received"`** on the exact page/tab it's attached to — this is a generic Chrome extension-messaging error, not unique to any one extension, and it fired during this session's own debugging even after the user disabled every *other* extension (including via Incognito). If this error shows up while diagnosing a page issue and an AI browser extension is attached to that tab, testing the same flow in a completely separate context (a different browser entirely, e.g. Safari, or the same browser with the automation extension fully disconnected) is the fastest way to rule it out as a false lead.
22. **`getActivity`'s board-less-row filter must explicitly list every action that logs with `Board:''`** (`login`, `login_failed`, `logout`, `setScriptConfig` — see `BOARDLESS_ACTIONS` in קוד.js) — a new such action added later and left out of that list will log correctly but silently never appear in any board's activity view. This exact gap (only `'login'` was special-cased, not `'login_failed'`) cost a long stretch of this session's own debugging: every real sign-in attempt actually *was* reaching the server and failing, but `getActivity` couldn't show why because the failure rows were being filtered out.
23. **Local testing landed a save on the real board-1 (`לוז מרכזי - אוריה`) during this session** — `resolveInitialBoardId()`'s default (no `?board=` param, no cached `localStorage` entry) is the legacy board, and it's also the account's own real board, so a fresh local sign-in with no explicit board param goes straight to it. No harm this time (verified: 11 weeks/6 people/6 topics intact after, char count grew normally) but it's a real near-miss, not a hypothetical — **always append `?board=<scratch-id>` explicitly to the local preview URL from the very first load**, don't rely on switching to a scratch board after the fact once signed in.
24. **`clasp run` is not usable in this environment** (`Exception: We're sorry, a server error occurred while reading from storage. Error code NOT_FOUND`) — the Apps Script API execution path isn't wired up for this project. Don't reach for it to run a function remotely; use the Apps Script editor UI directly (see #19), or add a gated API action if the operation needs to be repeatable (see the `setScriptConfig` Decisions row).
25. **Storage hardening list, ordered by value/effort** (from the 2026-09-16 research — full report [`docs/storage-recommendation-2026-09-16.md`](docs/storage-recommendation-2026-09-16.md), decision in Decisions above): (1) ~~`getBoardState` should read only the target board's row~~ **done, v15**; (2) ~~`listBoards` shouldn't read State columns at all~~ **done, v15**; (3) ~~a hash-based `getBoardMeta`/`ifHash` poll~~ **done, v15 + frontend 2026-09-17**; (4) verify the Google ID token **only at login**, never on the poll path — **already true**, `verifyGoogleIdToken` is only ever called from `login`, nothing to change; (5) batch/sample `Log` appends for `saveBoard` rather than one per debounced save — **not built**, deferred (lowest value/effort item on the list, no user-facing symptom yet). **Re-evaluate the "stay on Sheets" decision if**: any single board's state exceeds ~300,000 chars (→ move that board's type to Drive JSON files); a `notes` board exceeds ~250,000 chars or notes-board count exceeds ~8 (→ per-page storage becomes mandatory regardless of backend); steady-state open tabs exceed ~40 or `locked`/HTML-interstitial responses (Watch Out #18) stop being rare (→ the 30-simultaneous-executions Apps Script ceiling is binding, raise the poll interval before reconsidering a rewrite); daily `saveBoard` volume exceeds ~5,000; or the project gains a second maintainer or a budget (changes the Firestore calculus, since the two real objections to it are one-person-inspectability and rewrite cost, not the technology).
26. **A synthetic keystroke sent immediately after a structural DOM change (e.g. Enter → new block → typed text) can land on the wrong/stale element** in this automated testing environment — the same `requestAnimationFrame`-suspension already documented in #9/#10 for the drag system, now confirmed for the notes outline editor too: an automated `Enter` then `type` with no pause between them produced an empty `<b></b>` block instead of the typed text once, because the focus-the-new-block `requestAnimationFrame` callback in `focusBlock()` hadn't run yet when the synthetic keystrokes arrived. A ~1s wait (or a screenshot, which forces a paint) between a structural op and the next synthetic input reproduced the correct behavior every time after. **Real users are not affected** — normal keyboard input flows through the browser's real event loop, which doesn't have this race. Only relevant when scripting/automating this editor for testing.
27. **This machine cannot run the iOS Simulator** — only Xcode command-line tools are installed, not the full Xcode app, so `mcp__Claude_Code_iOS_Simulator__control{action:'attach'}` refuses with a clear error. Fix (needs the machine owner, not doable from a session): install Xcode from the Mac App Store, then `sudo xcode-select -s /Applications/Xcode.app/Contents/Developer`. Until then, "add to home screen and look at the icon" on iOS can only be checked manually on a real phone — do that at least once for the PWA work shipped 2026-09-17, since every other check (manifest/icon files serving correctly) is necessary but not sufficient proof the icon actually renders right in iOS's home-screen picker.
28. **A brand-new file path can 404 for under a minute right after a GitHub Pages deploy, even though `index.html` at the same commit already serves fresh** — seen shipping the PWA manifest/icons: the Pages build (`gh api repos/<owner>/<repo>/pages/builds/latest`) had already finished successfully, but the CDN edge needed a short extra window to start serving the newly-added paths. Distinct from the existing `max-age=600` staleness note (that's an old file serving *old* content; this is a new file briefly serving *no* content) — don't mistake one for the other. If a newly-added asset 404s right after a push, check the Pages build status via that API call before assuming something is actually broken.
29. **A design review of the notes-board polish work (2026-09-17, full report `.design/notes-board-polish/DESIGN_REVIEW.md`) found two real bugs code review alone missed**: (1) `.notes-card` needed `min-width:0` — without it a long unbroken title forced the grid item past its single-column track width on mobile (classic CSS Grid "automatic minimum size" gotcha, the grid equivalent of flexbox's `min-width:auto` issue); (2) `.notes-page-menu-btn`'s SVG had no explicit width/height and rendered at 0×0 on **every** viewport, not just mobile — the in-page "⋯" duplicate/rename/delete menu has been genuinely invisible since it was added earlier the same session. Both fixed same-session (commit `47729e5`). **`.week-nav` mobile overflow** (flagged here as out-of-scope/needs-go-ahead) was then independently reported by the user from their real phone minutes later — fixed same day (commit `24ca0f5`): `.week-nav` now allows `flex-wrap:wrap` (harmless on desktop, one line either way) and `.week-range`'s pointless `min-width:140px` drops to `0` under the existing 640px breakpoint, so a narrow screen wraps to a clean two-line group instead of forcing the whole page to scroll horizontally. **Also flagged**: no visible formatting UI at all on desktop — `.ob-toolbar` is `pointer:coarse`-only (`@media(pointer:fine){display:none!important}`), so heading/bold/italic/underline/indent are keyboard-shortcut-only for a mouse user, with zero visible affordance that they exist. Real gap, needs a scoped design proposal before implementing (not a quick CSS fix like the two bugs above).
